server:
  port: 9876
  servletPath: /app
management:
  port: 9877
spring:
  application:
    name: testzuulserver
error:
  path: ${server.servletPath}/error

zuul:
  server:
    route:
      testclient: /testing123
      stores: /stores
      customers: /customers

